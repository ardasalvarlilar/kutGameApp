{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": ".",
    "noEmit": true,
    // types: [] — motorun hicbir ortam tipine ihtiyaci olmadigini garanti eder.
    // Node ya da DOM API'si kullanan bir satir buraya girerse derleme kirilir.
    "types": []
  },
  "include": ["src/**/*.ts"]
}
